---
# The content of this file will be generated using helm template command
#
# helm template cndro autoscaler/cluster-autoscaler --namespace "kube-system" \
#       --set "autoDiscovery.clusterName=<EKS_CLUSTER_NAME>" \
#       --set "awsRegion=<AWS_REGION>" \
#       --set "rbac.serviceAccount.name=cluster-autoscaler-sa" \
#       --set "rbac.serviceAccount.annotations.eks\\.amazonaws\\.com/role-arn=<EKS_CLUSTER_AUTOSCALER_ROLE_ARN>"
#
# All parameters needed in the command are exposed as outputs from terraform
# In order to see them you can run: `terraform output`
